<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bingsum.mapper.OpenAreaMapper">

    <select id="getAreaInfoList" resultType="com.bingsum.util.ParaData">
        select * from open_area where delete_time is null
    </select>

    <select id="getAreaInfo" resultType="com.bingsum.util.ParaData">
        select * from open_area where delete_time is null and id=#{id}
    </select>

    <insert id="newAreaInfo" parameterType="com.bingsum.util.ParaData">
        insert into open_area(province,city,area,longitude,latitude,create_time) values (#{province},#{city},#{area}, #{longitude}, #{latitude}, NOW());
    </insert>

    <update id="setAreaInfo" parameterType="com.bingsum.util.ParaData">
        update open_area set province = #{province},city = #{city},area = #{area},longitude = #{longitude},latitude = #{latitude},update_time = NOW() where id = #{id};
    </update>

    <update id="delArea" parameterType="com.bingsum.util.ParaData">
        update open_area set delete_time = NOW() where id = #{id};
    </update>

</mapper>